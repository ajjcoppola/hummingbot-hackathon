---
description: Agent Builders Cup — Orca tight-range entry constraints
alwaysApply: true
---

# hummingbot-hackathon

- Venue is **Orca / Solana** only. Do not add Gate, Bitget, Binance Global, Hyperliquid, or Derive execution.
- Policy lives in `src/orca_tight_range/logic.py`. Controllers and backtests call `decide()`; do not fork a second policy.
- Condor is the local harness (`/gateway`, `/lp`, `/bots`, `/web`). LLM / `/agent` must not place or cancel LP. Execution is Hummingbot `LPExecutor` + Gateway.
- Do not use `~/proj/hummingbot` (2024). Install Condor + API + Gateway per `docs/INSTALL_PLAN.md`.
- Never invent a `pool_address`. Never commit wallet keys.
- Record parameter changes in `docs/TRIALS_LEDGER.md`. Refine until finals; registration/judging window ends **2026-09-30**; finals start **2026-10-01** (no hard Aug-31 freeze — see Botcamp timeline).
- Keep `submission/strategy.md` honest: no live P&L claims without a trial row.
